# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors / IDEs
.idea/
.vscode/
*.code-workspace
*.iml
*.swp

# Secrets — never commit
.env
.env.*
!.env.example
supabase/.env.local
supabase/.env

# Flutter build artefacts (defence in depth — app/.gitignore already covers these)
app/build/
app/.dart_tool/
app/.flutter-plugins-dependencies
app/.flutter-plugins
app/pubspec_overrides.yaml

# Logs / temp
*.log
*.tmp
