import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    // Widget tests will be added alongside individual features.
    // This placeholder keeps `flutter test` green during scaffolding.
    expect(1 + 1, 2);
  });
}
