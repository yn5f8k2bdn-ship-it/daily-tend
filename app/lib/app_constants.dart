/// Working-title app name. Single source of truth — when the brand lands,
/// change this one constant and every user-visible reference updates.
/// Internal package IDs (`dev.threezones.three_zones`) stay neutral forever.
const String kAppName = 'Daily Tend';
